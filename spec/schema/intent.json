{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://shapes.fyi/schema/v0.1.0/intent.json",
  "title": "Intent",
  "description": "Describes the purpose and motivation of a node. An open map: any additional properties beyond the defined fields are collected as extra metadata.",
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "description": "Classification of the intent (e.g. \"feature\", \"bugfix\", \"refactor\")."
    },
    "summary": {
      "type": "string",
      "description": "Brief human-readable summary of what this node intends."
    },
    "source": {
      "description": "Origin of this intent. Free-form value (string, object, array, etc.)."
    },
    "uris": {
      "type": "array",
      "items": { "type": "string" },
      "description": "URIs providing additional context or references for this intent."
    }
  },
  "required": ["kind", "summary", "source"],
  "additionalProperties": true
}
